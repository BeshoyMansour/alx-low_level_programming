#include "main.h"
#include <stdio.h>

/**
 * _puts - Print a string folllowed by a new line.
 *
 * @str: String to be printed
 *
 * Return: Nothing
 */

void _puts(char *str)
{
int i;
int i = 0;
while (*str[i] != '\0')
_putchar (i0);
}
